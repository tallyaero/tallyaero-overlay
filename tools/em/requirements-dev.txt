# Dev / test dependencies for tallyaero_em_diagram
# Install with:  pip install -r requirements.txt -r requirements-dev.txt
#
# These are NOT bundled into the PyInstaller binary (Phase 6).

pytest==8.3.3
pytest-snapshot==0.9.0
pip-tools==7.4.1
